#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: the string
 * Return: length of a string.
 */

int _strlen(char *s)
{
	int x = 0;
	int len = 0;

	for (; x[s] != '\0'; x++)
	{
		len++;
	}
	return (len);
}

